import Img from 'gatsby-image';
import React from 'react';

const GrayscaleLogo = ({ logoFixed }) => {
  return <Img fixed={logoFixed} alt={'Foret grayscale logo image'} />;
};

export default GrayscaleLogo;
